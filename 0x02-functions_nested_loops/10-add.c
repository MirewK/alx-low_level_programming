#include "main.h"
/**
 * add -adds two integers
 * x: x is  The first integer to be added.
 * y: y is The second integer to be added.
 * Return: Always 0.
 *
 */
int add(int x, int y)
{
	return (x + y);
}
